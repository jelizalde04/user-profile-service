name: CI/CD for Update User Password Microservice

on:
  push:
    branches:
      - test
    paths:
      - 'update-user-password-microservice/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker
        run: |
          sudo apt-get update
          sudo apt-get install -y docker.io
          sudo systemctl start docker
          sudo systemctl enable docker

      - name: Build Docker image
        run: |
          cd update-user-password-microservice
          docker build -t update-user-password-microservice .

      - name: Log in to AWS ECR
        run: |
          aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin <your-ecr-repo-url>

      - name: Tag Docker image
        run: |
          cd update-user-password-microservice
          docker tag update-user-password-microservice:latest <your-ecr-repo-url>/update-user-password-microservice:latest

      - name: Push Docker image
        run: |
          cd update-user-password-microservice
          docker push <your-ecr-repo-url>/update-user-password-microservice:latest

      - name: Deploy to EC2
        run: |
          ssh -o StrictHostKeyChecking=no ec2-user@<your-ec2-ip> "
            docker pull <your-ecr-repo-url>/update-user-password-microservice:latest
            docker stop update-user-password-microservice || true
            docker rm update-user-password-microservice || true
            docker run -d -p 6003:6003 --name update-user-password-microservice <your-ecr-repo-url>/update-user-password-microservice:latest
          "